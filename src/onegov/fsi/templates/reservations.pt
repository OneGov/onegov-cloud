<div metal:use-macro="layout.base" i18n:domain="onegov.fsi">
    <tal:b metal:fill-slot="title">
        ${layout.title}
    </tal:b>
    <tal:b metal:fill-slot="content">
        <div class="row">
            <div class="columns small-12">
                <div class="row">
                    <div class="columns small-12">
                        <strong tal:condition="not: reservations" i18n:translate="">No entries found</strong>
                        <tal:b tal:condition="reservations|nothing">
                                <table class="subscription-table" style="width: 100%">
                                    <thead>
                                        <th i18n:translate="">Attendee</th>
                                        <th i18n:translate="" tal:condition="not: layout.model.course_event_id">Course Name</th>
                                        <th i18n:translate="">Date</th>
                                        <th i18n:translate="">Course Status</th>
                                        <th i18n:translate="">Course attended</th>
                                        <th i18n:translate="">Last info mail</th>
                                        <th i18n:translate="" class="no-print">Manage</th>
                                    </thead>
                                    <tbody>
                                        <tal:b tal:repeat="reservation reservations">
                                            <tr>
                                                <td><a href="${layout.link(reservation)}">${str(reservation)}</a></td>
                                                <td tal:condition="not: layout.model.course_event_id"><a href="${request.link(reservation.course_event)}">${reservation.course_event.name}</a></td>
                                                <td>${layout.format_date(reservation.course_event.start, 'date')}</td>
                                                <td>${layout.format_status(reservation.course_event.status)}</td>

                                                <td>
                                                    <tal:b metal:use-macro="layout.macros['confirmation_slider']" tal:define="switch_id 'confirmation-switch-{}'.format(repeat.reservation.number); url layout.confirmation_link(reservation)" />
                                                </td>


                                                <td tal:define="sent reservation.info_sent">${layout.format_date(sent, 'date') if sent else ''}</td>
                                                <td class="no-print">
                                                    <tal:b tal:switch="reservation.course_event.is_past">
                                                        <metal:b tal:case="False" tal:define="item reservation" use-macro="layout.macros['intercooler_btn_for_item']"/>
                                                        <button tal:case="True" class="button tiny secondary disabled archived" i18n:translate="">Delete</button>
                                                    </tal:b>
                                                    <tal:b tal:condition="request.is_manager">
                                                        <a tal:condition="request.is_manager" href="${request.link(reservation, name='edit')}" class="button tiny secondary" i18n:translate="">Edit</a>
                                                    </tal:b>

                                                </td>
                                            </tr>
                                        </tal:b>
                                    </tbody>
                                </table>
                                <tal:b tal:condition="request.is_manager and layout.course_event and not layout.course_event.is_past">
                                    <a href="${layout.preview_info_mail_url}" class="button success no-print" i18n:translate="">Send Info Mail</a>
                                </tal:b>
                        </tal:b>
                    </div>
                </div>
            </div>
        </div>
    </tal:b>
</div>