<div metal:use-macro="layout.base" i18n:domain="onegov.org">
    <tal:b metal:fill-slot="title">
        ${title}
    </tal:b>
    <tal:b metal:fill-slot="content">
        <div metal:use-macro="layout.macros['form']" tal:condition="form" />
        <table class="link-check-stats" tal:condition="stats">
            <thead>
                <tr>
                    <th width="150" i18n:translate="">Total</th>
                    <th width="150" i18n:translate="">Healthy</th>
                    <th i18n:translate="">!200</th>
                    <th i18n:translate="">Errors</th>
                    <th i18n:translate="">Duration [s]</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>${stats.total}</td>
                    <td>${stats.ok}</td>
                    <td>${stats.nok}</td>
                    <td>${stats.error}</td>
                    <td>${round(stats.duration, 1)}</td>
                </tr>
            </tbody>
        </table>
        <table class="link-check-results">
          <thead>
            <tr>
              <th width="150" i18n:translate="">Model</th>
                <th width="150" i18n:translate="">Status</th>
                <th i18n:translate="">URL</th>
            </tr>
          </thead>
          <tbody>
            <tr tal:repeat="result check_responses">
                <td><a href="${result.link}" target="_blank">${result.name}</a></td>
                <td>${result.message or ''}</td>
                <td tal:define="url result.url">
                  <span class="link-status nok ${internal_link(url) and 'internal'}">
                      <a href="${url}" target="_blank">${truncate(url)}</a>
                  </span>
                </td>
            </tr>
          </tbody>
        </table>
    </tal:b>
</div>