<div metal:use-macro="layout.base" i18n:domain="onegov.town6">
    <tal:b metal:fill-slot="title">
        ${title}
    </tal:b>
    <tal:b metal:fill-slot="content">
        <div class="boardlet-group grid-x grid-padding-x columns small-up-1 medium-up-2 large-up-3 align-center" tal:repeat="group model.boardlets()">
            <div class="boardlet subpage-cards cell boardlet-${boardlet.state}" tal:repeat="boardlet group">
                <div class="card">
                    <div class="card-section">
                        <h2 class="h4">${boardlet.title}</h2>
                        <ul class="dense">
                            <li tal:repeat="fact boardlet.facts" class="${fact.css_class} small">
                                <i class="fa fa-fw ${fact.icon}" aria-hidden="true"></i>
                                <span class="boardlet-fact">
                                    ${fact.text}
                                </span>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>        
    </tal:b>
</div>
